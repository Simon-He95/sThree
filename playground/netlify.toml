[build]
publish = "dist"
command = "corepack enable && pnpm install --frozen-lockfile && pnpm run build"

[build.environment]
NODE_VERSION = "20"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
